==============
| Known bugs |
==============

*** (на проверку) dbroot.sh не находит
** Не заботает проверка на уже запущенный скрипт
* (на проверку) на scp1 есть пользователи у которых хомяк /home/u1234567, а не /var/www/u1234567, который является симлинком, но указан в /etc/passwd, поэтому скрипт на бэкапе пытается искать /home (надо сделать проверку пути на симлинк, и если что заменять хомяк каталогом, на который ссылается симлинк) 
* u2165317_zhile: ERROR 1049 (42000): Unknown database 'u2165317_zhile'
[ OK ] (пишет полную ошибку и OK в конце)



? Не работает создание архива (-A), если пользватель уже удален
? -s и -S не обрабатывают автоподдомены

==============
|    ToDo    |
==============

** После выполнения скрипта выводить текст с подробным ответом что и куда было восстановлено или выдано.
**(отдельный скрипт) Возможность создания дампа текущего состояния аккаунта вместо выдачи (-m --make_backup)
**(на проверку) Проверить наличие БД на хостинге до скачивания
** -S, если нет сайта - выдавать предупреждение и ПРОПУСКАТЬ

* Оптимизировать парсинг (сделать по панелям, а не парсить apache.conf)
* Продумать, как проверять и сравнивать, что есть на сайте, а что на бэкапе
* ключ (-n --no_backup) - не резервировать файлы пользователя при замене существующего

? DirectAdmin
